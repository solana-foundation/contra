# Railway configuration for Solana Operator (escrow program)
#
# Required environment variables:
#   DATABASE_URL              - PostgreSQL connection string
#   COMMON_RPC_URL            - Solana RPC endpoint (overrides rpc_url below)
#   COMMON_ESCROW_INSTANCE_ID - Escrow instance pubkey
#   ADMIN_SIGNER              - Signer type (memory/vault/turnkey/privy)
#   ADMIN_PRIVATE_KEY         - Admin private key
# Optional:
#   OPERATOR_SIGNER           - Operator signer type (falls back to admin)
#   OPERATOR_PRIVATE_KEY      - Operator private key

[common]
program_type = "escrow"
rpc_url = "https://api.mainnet-beta.solana.com"

[storage]
type = "postgres"
max_connections = 10

[operator]
poll_interval_secs = 1
batch_size = 1000
retry_max_attempts = 3
retry_base_delay_secs = 1
channel_buffer_size = 1000
# Solana Private Channels block time is ~100 ms; poll faster than the 400 ms default
confirmation_poll_interval_ms = 100
