apiVersion: 1
contactPoints:
  - orgId: 1
    name: webhook-alerts
    receivers:
      - uid: webhook-alerts
        type: webhook
        settings:
          url: ${ALERT_WEBHOOK_URL}
          httpMethod: POST
