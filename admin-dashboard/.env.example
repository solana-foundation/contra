# Admin API server URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# Must match ADMIN_API_TOKEN on the API server (leave empty for dev mode)
NEXT_PUBLIC_ADMIN_API_TOKEN=

# Set to "true" to use mock data (no API server needed)
NEXT_PUBLIC_DEMO=false
